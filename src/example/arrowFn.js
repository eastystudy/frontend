function 이름1(매개변수){
    rturn 반환값
}

function 이름2 = (매개변수) => {
    return 반환값
}

function 이름3 = 매개변수 => {
   return 반환값 
}

const 이름4 = 매개변수 => 반환값

const 이름5 = 매개변수 => {a:1}

//return 이 생략되어있음
const 이름6 = 매개변수 => []